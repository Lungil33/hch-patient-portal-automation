{
  "name": "hch-patient-portal-automation",
  "version": "1.0.0",
  "description": "Playwright E2E tests for the HCH patient-facing portal — appointment booking flow",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "@types/node": "^20.0.0",
    "dotenv": "^16.4.5",
    "typescript": "^5.4.5"
  }
}
